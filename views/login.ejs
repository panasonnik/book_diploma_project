<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <div class="content">
        <div class="form__header">
          <div class="logo logo--background form__header__logo">
            <div class="logo-good">GOOD</div>
            <div class="logo-book">BOOK</div>
          </div>
          <h1 class="heading heading--bold">Login</h1>
        </div>
        <form class="form mt-5" action="/auth/login" method="POST">
          <div class="form__field">
            <label class="form__field__label" for="usernameOrEmail"
              >Username or Email:</label
            >
            <input
              class="form__input-field"
              type="text"
              id="usernameOrEmail"
              name="usernameOrEmail"
              value="<%= data?.usernameOrEmail || '' %>"
              required
            />
            <% if (errorUsername) { %>
            <span class="error-message" id="username-error" role="alert">
              <%= errorUsername %>
            </span>
            <% } %>
          </div>
          <div class="form__field">
            <label class="form__field__label" for="password">Password:</label>
            <input
              class="form__input-field"
              type="password"
              id="password"
              name="password"
              required
            />
            <% if (errorPassword) { %>
            <span class="error-message" id="password-error" role="alert">
              <%= errorPassword %>
            </span>
            <% } %>
          </div>
          <% if (errorDB) { %>
          <span class="error-message" role="alert"> <%= errorDB %> </span>
          <% } %>
          <button type="submit" class="submit-btn">Login</button>
        </form>
        <div class="form__login-link">
          <span>Do not have an account?</span>
          <a href="/" class="form__login-link__link">Register</a>
        </div>
      </div>
    </div>
  </body>
</html>
